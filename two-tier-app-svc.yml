apiVersion: v1
kind: Service
metadata:
  name: two-tier-app-service
  namespace: app
  labels:
    app.kubernetes.io/instance: two-tier-app
spec:
  type: LoadBalancer
  selector:
    app: two-tier-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 5000
